# 课题开题报告

## 一、 项目基本信息

- **项目名称**：链安智御：面向银行大模型生产应用的全链路安全管控平台
- **参赛赛道**：金融安全服务赛道
- **核心定位**：依托联盟链规则化强制执行能力与 AI 全链路风险智能识别能力，针对银行信贷审批、风控辅助、智能客服等高风险大模型应用场景，提供“事前毫秒级硬拦截、事中语义级脱敏、事后四节点精准定责”的全链路安全合规闭环平台。
- **技术路线**：Go (Gin / GORM) + FISCO BCOS (Solidity) + Python (FastAPI / Qwen2) + Vue 3 (ECharts)

## 二、 立项背景与行业痛点

### 2.1 立项背景

随着生成式人工智能（LLM）的爆发，银行业正加速将大模型引入信贷审批、风控辅助、智能客服等核心业务。然而，金融行业对**数据安全、合规风控与权责划分**有着极高的监管要求。大模型固有的“黑盒”特性与幻觉问题，使得银行在落地应用时面临“想用而不敢用”的困境。

现有的金融安全规范（如《JR/T 0197-2020 金融数据安全 数据安全分级指南》、《生成式人工智能服务管理暂行办法》）均对敏感数据防护、权限管控及事后审计提出了硬性要求。

### 2.2 核心解决的五大痛点

| **序号** | **行业痛点**              | **本平台解决方案**                                           |
| -------- | ------------------------- | ------------------------------------------------------------ |
| **1**    | **Prompt 注入与越狱攻击** | **输入侧 AI 智能防线**：轻量微调模型 + 正则规则，毫秒级识别指令注入与意图越界。 |
| **2**    | **客户敏感数据泄露**      | **输出侧 NER 敏感脱敏**：NER 实体识别与正则引擎，对卡号、身份证、余额等自动打码掩码。 |
| **3**    | **内部员工越权滥用**      | **链上分级权限硬拦截**：角色/密级/时段/频次规则链上固化，Redis 缓存实现毫秒级拦截。 |
| **4**    | **合规失控与违规承诺**    | **链上策略统一终审**：金融监管规则全网链上生效，违反策略自动拦截或触发人工复核。 |
| **5**    | **AI 链路黑盒与无法定责** | **四节点分布式对账**：访问、RAG、推理、数仓四节点 Hash 链上比对，精准定位异常离群节点。 |

## 三、 总体方案与架构设计

### 3.1 核心设计理念：“事前硬拦截，事后异步定责”

针对传统区块链项目将写交易同步放入主请求管道导致高时延的通病，本平台采用“事前只读缓存/查链硬拦截 + 事后消息队列异步写链对账”的高并发架构，保证主业务响应时延控制在 10ms 级，兼顾性能与安全合规。

### 3.2 系统管道执行流程

```
[ 用户请求 / 攻防测试 ]
          │
          ▼
【1. Go 高性能安全网关】
          │──▶ 1.1 调 Python API：输入 Prompt 注入/越狱检测 & 用户动态风险评分
          │──▶ 1.2 查 Redis 缓存：链上准入策略校验（角色/数据密级/频次限流，< 2ms）
          │
          ▼ (校验通过)
【2. LLM 推理 / RAG 检索节点】
          │
          ▼
【3. 返回结果安全后置处理】
          │──▶ 3.1 调 Python API：敏感数据掩码脱敏 (NER+正则) & 语义合规评分
          │──▶ 3.2 查 Redis 缓存：链上合规策略终审
          │
          ▼
【4. 毫秒级响应安全结果给用户】
          │
          ▼ (异步 Goroutine / RabbitMQ 消息队列，不卡顿主流程)
【5. 链上异步对账与存证服务】
          └──▶ 收集 4 节点 Hash 提交至对账智能合约 ──▶ 触发链上对账事件 ──▶ 可视化大屏展示
```

## 四、 研究/开发内容与技术选型

### 1. 区块链与链上治理层（FISCO BCOS + Solidity）

- **底座**：采用 FISCO BCOS v3.x 金融级开源联盟链。
- **三大核心智能合约**：
  - **分级权限准入合约**：实现角色校验、数据密级匹配与滑动时间窗口限流。
  - **合规策略执行合约**：实现策略版本控制与高风险操作人工复核状态机。
  - **多节点对账定责合约**：实现四节点 Hash 比对，自动识别并标记离群异常节点。

### 2. 安全网关与业务后端层（Go + Gin + GORM + Redis + RabbitMQ）

- **安全网关**：基于 Go 语言（Gin 中间件）构建高性能拦截管道。
- **缓存与限流引擎**：利用 Redis 预加载链上策略，实现高并发 Token 桶限流与权限查询。
- **异步对账 Consumer**：监听 RabbitMQ/Redis Stream，异步触发 Go-SDK 调用对账合约上链。
- **审计 RESTful API**：编写溯源拓扑图数据接口、离群节点统计 API 及告警日志接口。

### 3. AI 安全与风控算法微服务（Python + FastAPI + PyTorch）

- **输入风险检测**：轻量化 Qwen2-1.5B 模型微调 + 规则引擎，拦截越狱与意图越界。
- **输出脱敏与合规**：SpaCy/HanLP NER 模型与正则表达式结合，实现敏感数据打码；语义模型评估违规理财话术。
- **动态风控引擎**：基于孤立森林（Isolation Forest）算法计算用户 0-100 动态风险评分。

### 4. 前端可视化演示层（Vue 3 + Element Plus + ECharts）

- **模拟攻防测试页**：实时演练恶意 Prompt 注入与越权请求的逐层拦截 Pipeline。
- **安全态势大屏**：绘制实时拦截量趋势、风险类型分布及高风险用户榜单。
- **审计溯源对账页**：绘制四大节点 Hash 连贯性拓扑图，直观红框标记篡改发生节点。

## 五、 团队分工与执行计划

### 5.1 细致分工表

| **组员**                         | **主要职责与模块划分**                                       | **主要使用技术**                                     |
| -------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------- |
| **组员 A** *(区块链侧/链下后端)* | **1. 智能合约**：编写 3 大 Solidity 核心合约。 **2. 链底座部署**：FISCO BCOS 环境搭建与 Go-SDK 绑定代码导出。 **3. Redis 策略缓存**：编写策略预加载与秒级查验 Go 模块。 **4. MQ 消费服务**：编写异步 Consumer 消费 Hash 并调用合约上链。 **5. 审计 API 开发**：编写对账拓扑图与审计溯源 RESTful 接口。 | Solidity, FISCO BCOS, Go (Gin/GORM), Redis, RabbitMQ |
| **组员 B** *(网关/业务/前端)*    | **1. Go 安全网关**：开发 Gin 拦截管道，串联 AI 模块与 Redis 校验。 **2. 业务后台**：开发用户登录、角色分配、数据分级配置 CRUD。 **3. Vue 3 前端开发**：完成模拟攻防页、安全态势大屏、审计溯源拓扑页。 | Go (Gin), Vue 3, Element Plus, ECharts               |
| **组员 C** *(AI 算法全权)*       | **1. Prompt 识别服务**：部署 FastAPI 微服务，实现越狱与注入检测。 **2. 输出脱敏服务**：开发 NER + 正则敏感信息掩码与合规评分引擎。 **3. 动态风控算法**：开发基于孤立森林的用户异常行为评分模型。 | Python 3.10, FastAPI, PyTorch, Qwen2, Scikit-learn   |

## 六、 项目创新点与特色

1. **理念创新：变“事后静态日志”为“事前规则硬拦截”**

   打破区块链仅作为事后日志库的局限，将数据密级与合规策略固化于链上，结合 Redis 高速缓存，在大模型回答生成前即完成毫秒级访问管控。

2. **工程创新：无感高并发的“异步四节点分布式对账”**

   提出涵盖访问、RAG、推理、数仓四节点的 Hash 连贯性校验机制。主请求流水通过消息队列异步解耦，既满足了银行级高并发低延时要求，又实现了可追溯的定责闭环。

3. **技术融合：AI 语义防护与区块链状态机的深度互补**

   AI 解决“非结构化自然语言的语义安全与脱敏”难题，区块链解决“结构化规则的确定性执行与多方信任”难题，形成双重防线。

## 七、 预期成果与交付形式

1. **软件系统源码**：包含 Go 安全网关/后台源码、Solidity 智能合约源码、Python AI 微服务源码及 Vue 3 前端源码。
2. **联盟链部署包**：FISCO BCOS Docker Compose 一键启动配置文件与智能合约 Go-SDK 包。
3. **演示系统 Demo**：具备攻防演练、动态态势大屏、四节点拓扑对账三个可运行展示的系统界面。
4. **技术文档**：包含《项目开题报告》、《API 接口规范文档》、《系统架构设计书》。